# github 代码提交流程
1. 将所有新文件添加到暂存区
```git add .```
2. 再次执行提交
```git commit -m "备注内容"```
3. 推送到 GitHub
```git push origin main```

# 在 Markdown (.md) 文件中，代码标注主要分为两种方式：行内代码和代码块。
1. 行内代码 (Inline Code)  
    一对反引号（Backticks，通常在键盘 Esc 键下方）将其包裹。
2. 代码块 (Code Blocks)
    使用三连反引号（ ``` ）作为开头和结尾